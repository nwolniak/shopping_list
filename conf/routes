# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET           /items/:itemId        controllers.ShoppingListController.getById(itemId: Long)

GET           /items                controllers.ShoppingListController.getAll()

POST          /items/:item          controllers.ShoppingListController.postItem(item: Item)

DELETE        /items/:itemId        controllers.ShoppingListController.deleteById(itemId: Long)